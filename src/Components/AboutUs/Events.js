import React from 'react';
import Navbar from '../Shared/Navbar/Navbar';

const Events = () => {
    return (
        <section>
            <Navbar />
            <h1>Events</h1>
        </section>
    );
};

export default Events;